# Exact versions used on the training/evaluation box (Python 3.12).
# The box uses CUDA builds (torch 2.11.0+cu128); on CPU-only machines install the
# plain CPU wheels of torch/torchvision (same versions, without the +cuXXX build tag).
torch==2.11.0
torchvision==0.26.0
segmentation-models-pytorch==0.5.0
timm==1.0.27
albumentations==2.0.8
opencv-python-headless==4.13.0.92
numpy==2.4.4
scipy==1.18.0
onnx==1.22.0
onnxruntime==1.27.0
matplotlib==3.11.0
pillow==12.2.0
pandas==3.0.3
tqdm==4.68.3
