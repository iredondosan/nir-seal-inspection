# Demo assets

Download the demo bundle from the GitHub Release and unpack here:
- `weights/seal.pt`, `weights/defect.pt` — deployed models
- `samples/*.png` — a few sample NIR packs

Then run `python demo/demo.py` or `make demo`.
